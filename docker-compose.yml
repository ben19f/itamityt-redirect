version: '3.3'

services:
  redirect:
    build:
      context: ../../
      dockerfile: docker/redirect/Dockerfile
    container_name: itamityt_redirect
    restart: always
    env_file:
      - .env
    ports:
      - "8200:8200"
    command: uvicorn main:app --reload  --host 0.0.0.0 --port 8200
